spring:
  application:
    name: myself-badwritten-sso

  cloud:
    nacos:
      config:
        #server-addr: my.nacos.com:8848
        #nacos集群配置(Nginx)
        server-addr: 127.0.0.1:8848
        prefix: sso-config
        file-extension: yaml
        group: dev
      discovery:
        username: nacos
        password: nacos
        namespace: public

server:
  port: 8081
